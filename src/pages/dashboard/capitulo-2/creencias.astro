---
import DashboardLayout from '../../../layouts/DashboardLayout.astro';
import Button from '../../../components/ui/Button.astro';
---

<DashboardLayout title="Martillo de Creencias - Capítulo 2">
  <div class="max-w-3xl mx-auto">
    <div class="mb-10">
      <div class="inline-block px-3 py-1 bg-primary/10 text-primary text-xs font-bold rounded-full mb-3 tracking-wider uppercase">
        Capítulo 2
      </div>
      <h1 class="text-3xl font-black text-slate-900 mb-4">
        Martillo de Creencias
      </h1>
      <p class="text-lg text-slate-600 leading-relaxed">
        Elige tu área más débil y destruye la creencia limitante que te mantiene ahí con evidencia real.
      </p>
    </div>

    <div class="bg-white rounded-2xl p-8 border border-slate-100 shadow-xl shadow-slate-200/50">
      <form id="beliefForm" class="space-y-8">
        
        <div>
          <label class="label-field">Área a Trabajar</label>
          <select name="area" class="input-field" required>
            <option value="" disabled selected>Selecciona un área</option>
            <option value="Salud">Salud y Energía</option>
            <option value="Finanzas">Finanzas y Riqueza</option>
            <option value="Relaciones">Amor y Relaciones</option>
            <option value="Espiritualidad">Espiritualidad y Propósito</option>
            <option value="Negocios">Carrera y Negocios</option>
          </select>
          <p class="text-xs text-slate-400 mt-2">Te sugerimos empezar por donde tengas menor puntaje.</p>
        </div>

        <div>
          <label class="label-field">La Creencia Limitante</label>
          <textarea name="limitingBelief" rows="3" class="input-field resize-none" placeholder="Ej: 'Soy demasiado viejo para empezar un negocio digital...'" required></textarea>
        </div>

        <div>
          <label class="label-field text-primary">Prueba de Error (Evidencia Contraria)</label>
          <textarea name="evidence" rows="4" class="input-field border-primary/20 focus:border-primary focus:ring-primary/20" placeholder="Ej: Ray Kroc fundó McDonald's a los 52 años. Harland Sanders franquició KFC a los 62..." required></textarea>
          <p class="text-xs text-slate-400 mt-2">Busca un caso real que demuestre que tu creencia es falsa.</p>
        </div>

        <div class="pt-6 border-t border-slate-100">
          <Button type="submit" class="w-full">
            Romper Creencia
          </Button>
        </div>

        <div id="message" class="hidden p-4 rounded-lg text-sm text-center"></div>
      </form>
    </div>
  </div>
</DashboardLayout>

<script>
  const form = document.getElementById('beliefForm') as HTMLFormElement;
  const messageDiv = document.getElementById('message') as HTMLDivElement;

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const formData = new FormData(form);
    const data = {
      area: formData.get('area'),
      limitingBelief: formData.get('limitingBelief'),
      evidence: formData.get('evidence'),
    };

    try {
      const response = await fetch(`${window.API_URL}/exercises/belief`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify(data),
      });

      if (!response.ok) throw new Error('Error al guardar');

      messageDiv.textContent = '¡Creencia destruida!';
      messageDiv.className = 'p-4 bg-green-50 text-green-600 rounded-lg text-sm text-center font-medium block mt-4';
      
      setTimeout(() => {
        window.location.href = '/dashboard/capitulo-2/paradigma';
      }, 1500);

    } catch (error) {
      messageDiv.textContent = 'Error al guardar.';
      messageDiv.className = 'p-4 bg-red-50 text-red-600 rounded-lg text-sm text-center font-medium block mt-4';
    }
  });
</script>
