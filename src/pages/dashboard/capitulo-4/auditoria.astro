---
import DashboardLayout from '../../../layouts/DashboardLayout.astro';
import Button from '../../../components/ui/Button.astro';
---

<DashboardLayout title="Auditoría de las 3 Rs - Capítulo 4">
  <div class="max-w-4xl mx-auto">
    <div class="mb-10">
      <div class="inline-block px-3 py-1 bg-primary/10 text-primary text-xs font-bold rounded-full mb-3 tracking-wider uppercase">
        Capítulo 4
      </div>
      <h1 class="text-3xl font-black text-slate-900 mb-4">
        Auditoría Semanal (Las 3 Rs)
      </h1>
      <p class="text-lg text-slate-600 leading-relaxed">
        La maestría requiere mantenimiento. Realiza este ejercicio cada domingo para ajustar tu brújula.
      </p>
    </div>

    <div class="bg-white rounded-2xl p-8 border border-slate-100 shadow-xl shadow-slate-200/50">
      <form id="auditForm" class="space-y-12">
        
        <!-- Review -->
        <div>
          <h3 class="text-xl font-bold text-slate-900 mb-4 flex items-center gap-2">
            <span class="w-8 h-8 bg-slate-100 rounded-full flex items-center justify-center text-sm">1</span>
            Revisar (Honestidad)
          </h3>
          <p class="text-sm text-slate-500 mb-3">¿Qué logré esta semana? ¿Dónde fallé? Sin juicios, solo datos.</p>
          <textarea name="review" rows="4" class="input-field" placeholder="Esta semana logré..." required></textarea>
        </div>

        <!-- Rethink -->
        <div>
          <h3 class="text-xl font-bold text-slate-900 mb-4 flex items-center gap-2">
            <span class="w-8 h-8 bg-slate-100 rounded-full flex items-center justify-center text-sm">2</span>
            Re-pensar (Ajuste)
          </h3>
          <p class="text-sm text-slate-500 mb-3">¿Qué estrategia debo cambiar? ¿Qué obstáculo debo eliminar?</p>
          <textarea name="rethink" rows="4" class="input-field" placeholder="Necesito ajustar..." required></textarea>
        </div>

        <!-- Rewrite -->
        <div>
          <h3 class="text-xl font-bold text-slate-900 mb-4 flex items-center gap-2">
            <span class="w-8 h-8 bg-slate-100 rounded-full flex items-center justify-center text-sm">3</span>
            Re-escribir (Claridad)
          </h3>
          <p class="text-sm text-slate-500 mb-3">Escribe tus metas para la próxima semana con total claridad.</p>
          <textarea name="rewrite" rows="4" class="input-field" placeholder="Mis objetivos son..." required></textarea>
        </div>

        <div class="pt-6 border-t border-slate-100">
          <Button type="submit" class="w-full">
            Guardar Auditoría
          </Button>
        </div>

        <div id="message" class="hidden p-4 rounded-lg text-sm text-center"></div>
      </form>
    </div>
  </div>
</DashboardLayout>

<script>
  const form = document.getElementById('auditForm') as HTMLFormElement;
  const messageDiv = document.getElementById('message') as HTMLDivElement;

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const formData = new FormData(form);
    const data = {
      review: formData.get('review'),
      rethink: formData.get('rethink'),
      rewrite: formData.get('rewrite'),
    };

    try {
      const response = await fetch(`${window.API_URL}/exercises/audit`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify(data),
      });

      if (!response.ok) throw new Error('Error al guardar');

      messageDiv.textContent = '¡Auditoría guardada!';
      messageDiv.className = 'p-4 bg-green-50 text-green-600 rounded-lg text-sm text-center font-medium block mt-4';
      
      setTimeout(() => {
        window.location.href = '/dashboard/capitulo-4/retos';
      }, 1500);

    } catch (error) {
      messageDiv.textContent = 'Error al guardar.';
      messageDiv.className = 'p-4 bg-red-50 text-red-600 rounded-lg text-sm text-center font-medium block mt-4';
    }
  });
</script>
