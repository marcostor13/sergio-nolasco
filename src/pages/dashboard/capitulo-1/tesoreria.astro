---
import DashboardLayout from '../../../layouts/DashboardLayout.astro';
import Input from '../../../components/ui/Input.astro';
import Button from '../../../components/ui/Button.astro';
---

<DashboardLayout title="Tesorería Personal - Capítulo 1">
  <div class="max-w-3xl mx-auto">
    <div class="mb-10">
      <div class="inline-block px-3 py-1 bg-primary/10 text-primary text-xs font-bold rounded-full mb-3 tracking-wider uppercase">
        Capítulo 1
      </div>
      <h1 class="text-3xl font-black text-slate-900 mb-4">
        El Estado de tu Tesorería
      </h1>
      <p class="text-lg text-slate-600 leading-relaxed">
        El dinero es una métrica de valor aportado. Para saber a dónde vas, primero debes saber dónde estás. Sé brutalmente honesto.
      </p>
    </div>

    <div class="bg-white rounded-2xl p-8 border border-slate-100 shadow-xl shadow-slate-200/50">
      <form id="financialForm" class="space-y-6">
        
        <!-- Net Income -->
        <div>
          <label class="block text-sm font-semibold text-slate-700 mb-2">Ingresos Netos Mensuales Reales</label>
          <div class="relative">
            <span class="absolute left-4 top-3.5 text-slate-400 font-bold">$</span>
            <input type="number" name="netIncome" class="input-field pl-8" placeholder="0.00" required>
          </div>
          <p class="text-xs text-slate-400 mt-2">Lo que realmente entra a tu bolsillo después de impuestos y costos operativos.</p>
        </div>

        <!-- Debts -->
        <div>
          <label class="block text-sm font-semibold text-slate-700 mb-2">Deudas y Compromisos Totales</label>
          <div class="relative">
            <span class="absolute left-4 top-3.5 text-slate-400 font-bold">$</span>
            <input type="number" name="debts" class="input-field pl-8" placeholder="0.00" required>
          </div>
          <p class="text-xs text-slate-400 mt-2">La suma de todo lo que debes hoy (tarjetas, préstamos, hipotecas).</p>
        </div>

        <!-- Net Worth -->
        <div>
          <label class="block text-sm font-semibold text-slate-700 mb-2">Patrimonio Neto (Activos - Pasivos)</label>
          <div class="relative">
            <span class="absolute left-4 top-3.5 text-slate-400 font-bold">$</span>
            <input type="number" name="netWorth" class="input-field pl-8" placeholder="0.00" required>
          </div>
          <p class="text-xs text-slate-400 mt-2">Si vendieras todo lo que tienes y pagaras todo lo que debes, ¿cuánto queda?</p>
        </div>

        <div class="pt-6 border-t border-slate-100">
          <Button type="submit" class="w-full">
            Guardar y Continuar
          </Button>
        </div>

        <div id="message" class="hidden p-4 rounded-lg text-sm text-center"></div>
      </form>
    </div>
  </div>
</DashboardLayout>

<script>
  const form = document.getElementById('financialForm') as HTMLFormElement;
  const messageDiv = document.getElementById('message') as HTMLDivElement;

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    messageDiv.classList.add('hidden');
    
    const formData = new FormData(form);
    const data = {
      netIncome: parseFloat(formData.get('netIncome') as string),
      debts: parseFloat(formData.get('debts') as string),
      netWorth: parseFloat(formData.get('netWorth') as string),
    };

    try {
      const response = await fetch(`${window.API_URL}/exercises/financial`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        credentials: 'include',
        body: JSON.stringify(data),
      });

      if (!response.ok) throw new Error('Error al guardar datos');

      messageDiv.textContent = '¡Datos guardados correctamente!';
      messageDiv.className = 'p-4 bg-green-50 text-green-600 rounded-lg text-sm text-center font-medium block mt-4';
      
      setTimeout(() => {
        window.location.href = '/dashboard/capitulo-1/tiempo';
      }, 1500);

    } catch (error) {
      messageDiv.textContent = 'Ocurrió un error. Inténtalo de nuevo.';
      messageDiv.className = 'p-4 bg-red-50 text-red-600 rounded-lg text-sm text-center font-medium block mt-4';
    }
  });
</script>
